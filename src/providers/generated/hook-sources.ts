// 本文件由 scripts/build-hooks.mjs 自动生成，请勿手动编辑。
// 内容为各平台网络拦截器的自包含 IIFE 源码（注入 AI 网页主世界执行）。

const DEEPSEEK_HOOK = "\"use strict\";\n(() => {\n  // src/providers/hooks/shared/sse.ts\n  function createFrameDecoder() {\n    var buffer = \"\", scanFrom = 0;\n    return {\n      push: function(text) {\n        buffer += text;\n        var frames = [], re = /\\r?\\n\\r?\\n/g, offset = 0, m;\n        re.lastIndex = scanFrom;\n        while ((m = re.exec(buffer)) !== null) {\n          frames.push(buffer.slice(offset, m.index));\n          offset = m.index + m[0].length;\n        }\n        buffer = buffer.slice(offset);\n        scanFrom = Math.max(0, buffer.length - 3);\n        return frames;\n      },\n      finish: function() {\n        var frames = [];\n        if (buffer) frames.push(buffer);\n        buffer = \"\";\n        scanFrom = 0;\n        return frames;\n      }\n    };\n  }\n  function extractData(block) {\n    if (!block || !block.trim()) return null;\n    var data = null;\n    var lines = block.split(/\\r\\n|\\r|\\n/);\n    for (var i = 0; i < lines.length; i++) {\n      var line = lines[i];\n      if (line.indexOf(\"data:\") === 0) {\n        var d = line.slice(5).trim();\n        data = data == null ? d : data + \"\\n\" + d;\n      }\n    }\n    return data;\n  }\n  function parseBlock(block) {\n    var data = extractData(block);\n    if (data == null) return null;\n    try {\n      return JSON.parse(data);\n    } catch (e) {\n      return null;\n    }\n  }\n\n  // src/providers/hooks/deepseek.ts\n  function install() {\n    var MARKER = \"__cuckooDeepseekHookInstalled__\";\n    if (window[MARKER]) return;\n    window[MARKER] = true;\n    var COMPLETION_PATH = \"/api/v0/chat/completion\";\n    var STOP_STREAM_PATH = \"/api/v0/chat/stop_stream\";\n    var userStopped = false;\n    var perf = {\n      t0: 0,\n      frames: 0,\n      consumeMs: 0,\n      maxConsume: 0,\n      bytes: 0,\n      longTasks: 0,\n      longTaskMs: 0,\n      maxLongTask: 0,\n      // 长任务明细：[{off: 相对流起点的偏移ms, dur: 时长ms}]，最多记 120 条\n      longList: [],\n      // chunk 到达时间点（相对流起点 ms），最多记 200 条\n      chunkTimes: [],\n      reset: function() {\n        this.t0 = performance.now();\n        this.frames = 0;\n        this.consumeMs = 0;\n        this.maxConsume = 0;\n        this.bytes = 0;\n        this.longTasks = 0;\n        this.longTaskMs = 0;\n        this.maxLongTask = 0;\n        this.longList = [];\n        this.chunkTimes = [];\n      },\n      addConsume: function(ms) {\n        this.consumeMs += ms;\n        if (ms > this.maxConsume) this.maxConsume = ms;\n      },\n      addChunkTime: function() {\n        if (this.chunkTimes.length < 200) this.chunkTimes.push(Math.round(performance.now() - this.t0));\n      },\n      summary: function() {\n        var now = performance.now();\n        return {\n          durMs: Math.round(now - this.t0),\n          frames: this.frames,\n          bytes: this.bytes,\n          consumeMs: Math.round(this.consumeMs),\n          consumePct: now - this.t0 > 0 ? Math.round(this.consumeMs / (now - this.t0) * 100) : 0,\n          maxConsumeMs: Math.round(this.maxConsume * 10) / 10,\n          longTasks: this.longTasks,\n          longTaskMs: Math.round(this.longTaskMs),\n          maxLongTaskMs: Math.round(this.maxLongTask),\n          longList: this.longList.slice(0, 120),\n          chunkTimes: this.chunkTimes.slice(0, 200)\n        };\n      }\n    };\n    try {\n      if (typeof PerformanceObserver !== \"undefined\") {\n        var _po = new PerformanceObserver(function(list) {\n          var es = list.getEntries();\n          for (var i = 0; i < es.length; i++) {\n            perf.longTasks++;\n            perf.longTaskMs += es[i].duration;\n            if (es[i].duration > perf.maxLongTask) perf.maxLongTask = es[i].duration;\n            if (perf.longList.length < 120) {\n              perf.longList.push({\n                off: Math.round(es[i].startTime - perf.t0),\n                dur: Math.round(es[i].duration)\n              });\n            }\n          }\n        });\n        _po.observe({ entryTypes: [\"longtask\"] });\n      }\n    } catch (e) {\n    }\n    function dumpDom() {\n      var out = [];\n      try {\n        var all = document.querySelectorAll(\"div, ul, ol, section\");\n        for (var i = 0; i < all.length && out.length < 40; i++) {\n          var el = all[i];\n          var kids = el.children;\n          if (!kids || kids.length < 5) continue;\n          var cls0 = kids[0] && kids[0].getAttribute ? kids[0].getAttribute(\"class\") || \"\" : \"\";\n          if (!cls0) continue;\n          var same = 0;\n          for (var j = 0; j < kids.length; j++) {\n            var cj = kids[j].getAttribute ? kids[j].getAttribute(\"class\") || \"\" : \"\";\n            if (cj === cls0) same++;\n          }\n          if (same >= kids.length * 0.8) {\n            var own = el.getAttribute ? el.getAttribute(\"class\") || \"\" : \"\";\n            out.push({\n              tag: el.tagName,\n              cls: String(own).slice(0, 100),\n              count: kids.length,\n              childTag: kids[0].tagName,\n              childCls: String(cls0).slice(0, 100)\n            });\n          }\n        }\n      } catch (e) {\n      }\n      return out;\n    }\n    function perfReport(path, extra) {\n      try {\n        window.dispatchEvent(new CustomEvent(\"cuckoo-perf\", { detail: Object.assign({ path, sessionId: getSessionIdFromUrl() }, perf.summary(), extra || {}, { dom: dumpDom() }) }));\n      } catch (e) {\n      }\n    }\n    function isStopStream(url, method) {\n      if (!url) return false;\n      if (String(method || \"GET\").toUpperCase() !== \"POST\") return false;\n      try {\n        var u = new URL(url, document.baseURI);\n        return u.pathname === STOP_STREAM_PATH;\n      } catch (e) {\n        return String(url).indexOf(STOP_STREAM_PATH) !== -1;\n      }\n    }\n    function isCompletion(url, method) {\n      if (!url) return false;\n      if (String(method || \"GET\").toUpperCase() !== \"POST\") return false;\n      try {\n        var u = new URL(url, document.baseURI);\n        return u.pathname === COMPLETION_PATH;\n      } catch (e) {\n        return String(url).indexOf(COMPLETION_PATH) !== -1;\n      }\n    }\n    function getSessionIdFromUrl() {\n      try {\n        var m = String(location.href).match(/\\/chat\\/s\\/([a-f0-9-]+)/i);\n        return m ? m[1] : null;\n      } catch (e) {\n        return null;\n      }\n    }\n    function resolveStatus(extractor) {\n      var st = \"error\";\n      if (extractor.finished) {\n        if (extractor.thinkLen > 0 && extractor.textLen === 0) st = \"error\";\n        else st = \"finished\";\n      } else if (userStopped) st = \"stopped\";\n      return st;\n    }\n    function dispatch(text, status, tokenUsage, msgIds, extra, dbg) {\n      try {\n        if (status === \"error\") {\n          var detail = { text: text || \"\", status: \"error\", tokenUsage: tokenUsage || null, msgIds: msgIds || null };\n          if (dbg) detail.dbg = dbg;\n          if (extra) {\n            for (var k in extra) {\n              if (Object.prototype.hasOwnProperty.call(extra, k)) detail[k] = extra[k];\n            }\n          }\n          window.dispatchEvent(new CustomEvent(\"cuckoo-ai-error\", { detail }));\n        } else {\n          var respDetail = { text: text || \"\", finished: status === \"finished\", status, tokenUsage: tokenUsage || null, msgIds: msgIds || null };\n          if (dbg) respDetail.dbg = dbg;\n          window.dispatchEvent(new CustomEvent(\"cuckoo-ai-response\", { detail: respDetail }));\n        }\n      } catch (e) {\n      }\n    }\n    function createExtractor() {\n      var fragmentTypes = [];\n      var currentIndex = -1;\n      var observed = false;\n      var text = \"\";\n      var thinkText = \"\";\n      var finished = false;\n      var incomplete = false;\n      var tokenUsage = null;\n      var msgIds = null;\n      var statusFrames = [];\n      function captureMsgIds(src) {\n        if (!src || typeof src !== \"object\") return;\n        if (!msgIds) msgIds = {};\n        if (typeof src.request_message_id === \"number\") msgIds.requestMessageId = src.request_message_id;\n        if (typeof src.response_message_id === \"number\") msgIds.responseMessageId = src.response_message_id;\n        if (typeof src.message_id === \"number\") msgIds.responseMessageId = src.message_id;\n        if (typeof src.parent_id === \"number\") msgIds.requestMessageId = src.parent_id;\n      }\n      function captureTokenUsage(src) {\n        if (!src || typeof src !== \"object\") return;\n        var changed = false;\n        if (!tokenUsage) tokenUsage = {};\n        if (typeof src.accumulated_token_usage === \"number\") {\n          tokenUsage.accumulatedTokens = src.accumulated_token_usage;\n          changed = true;\n        }\n        if (typeof src.inserted_at === \"number\") {\n          tokenUsage.insertedAt = src.inserted_at;\n          changed = true;\n        }\n        if (typeof src.updated_at === \"number\") {\n          tokenUsage.updatedAt = src.updated_at;\n          changed = true;\n        }\n        if (typeof src.model_type === \"string\") {\n          tokenUsage.modelType = src.model_type;\n          changed = true;\n        }\n        if (!changed && Object.keys(tokenUsage).length === 0) tokenUsage = null;\n      }\n      function lastSeg(p) {\n        return typeof p === \"string\" ? p.split(\"/\").pop() : \"\";\n      }\n      function isTextPatch(p) {\n        var s = lastSeg(p);\n        return s === \"content\" || s === \"text\" || s === \"markdown\" || s === \"delta\";\n      }\n      function isResponsePatch(p) {\n        return typeof p === \"string\" && (p === \"response\" || p.indexOf(\"response/\") === 0);\n      }\n      function isResponseTextPatch(p) {\n        return isTextPatch(p) && isResponsePatch(p);\n      }\n      function isThinkingPatch(p) {\n        var s = lastSeg(p);\n        return s === \"reasoning_content\" || s === \"thinking_content\";\n      }\n      function isFragmentsAppend(p) {\n        return p && typeof p.p === \"string\" && p.p.slice(-10) === \"/fragments\" && p.o === \"APPEND\" && Array.isArray(p.v);\n      }\n      function snapshotFragments(p) {\n        if (!p || p.p !== void 0 || !p.v || typeof p.v !== \"object\") return null;\n        var r = p.v.response;\n        if (!r || typeof r !== \"object\") return null;\n        var f = r.fragments;\n        return Array.isArray(f) && f.length > 0 ? f : null;\n      }\n      function fragText(f) {\n        if (!f || typeof f !== \"object\") return \"\";\n        if (typeof f.content === \"string\") return f.content;\n        if (typeof f.text === \"string\") return f.text;\n        return \"\";\n      }\n      function typeAt(i) {\n        if (i === -1) i = currentIndex;\n        if (i < 0 || i >= fragmentTypes.length) return null;\n        return fragmentTypes[i];\n      }\n      function isThink(t) {\n        return String(t).toUpperCase() === \"THINK\";\n      }\n      function consumeFragmentContent(fragments, types) {\n        for (var i = 0; i < fragments.length; i++) {\n          var c = fragText(fragments[i]);\n          if (!c) continue;\n          if (isThink(types[i])) thinkText += c;\n          else text += c;\n        }\n      }\n      function consume(parsed) {\n        if (!parsed || typeof parsed !== \"object\") return;\n        if (parsed.o === \"BATCH\" && Array.isArray(parsed.v)) {\n          for (var i = 0; i < parsed.v.length; i++) consume(parsed.v[i]);\n          return;\n        }\n        captureMsgIds(parsed);\n        if (parsed.v && typeof parsed.v === \"object\" && parsed.v.response && typeof parsed.v.response === \"object\") {\n          captureTokenUsage(parsed.v.response);\n          captureMsgIds(parsed.v.response);\n        }\n        if (typeof parsed.p === \"string\" && lastSeg(parsed.p) === \"accumulated_token_usage\" && typeof parsed.v === \"number\") {\n          captureTokenUsage({ accumulated_token_usage: parsed.v });\n        }\n        if (parsed.updated_at !== void 0) {\n          captureTokenUsage(parsed);\n        }\n        if (isFragmentsAppend(parsed)) {\n          var types = [];\n          for (var j = 0; j < parsed.v.length; j++) {\n            types.push(String(parsed.v[j] && parsed.v[j].type || \"RESPONSE\"));\n          }\n          for (var ti = 0; ti < types.length; ti++) fragmentTypes.push(types[ti]);\n          currentIndex = fragmentTypes.length - 1;\n          observed = true;\n          consumeFragmentContent(parsed.v, types);\n          return;\n        }\n        var snap = snapshotFragments(parsed);\n        if (snap) {\n          var first = !observed;\n          var stypes = [];\n          for (var k = 0; k < snap.length; k++) {\n            stypes.push(String(snap[k] && snap[k].type || \"RESPONSE\"));\n          }\n          fragmentTypes = stypes;\n          currentIndex = fragmentTypes.length - 1;\n          observed = true;\n          if (first) consumeFragmentContent(snap, stypes);\n          return;\n        }\n        if (isThinkingPatch(parsed.p) && typeof parsed.v === \"string\") return;\n        if (typeof parsed.p === \"string\" && isResponseTextPatch(parsed.p) && typeof parsed.v === \"string\") {\n          var m = /^response\\/fragments\\/(-?\\d+)\\//.exec(parsed.p);\n          var idx = m ? Number(m[1]) : -1;\n          if (isThink(typeAt(idx))) thinkText += parsed.v;\n          else text += parsed.v;\n          return;\n        }\n        if (parsed.p === void 0 && typeof parsed.v === \"string\") {\n          if (isThink(typeAt(currentIndex))) thinkText += parsed.v;\n          else text += parsed.v;\n          return;\n        }\n        if (parsed.p === \"response/status\" || parsed.p === \"quasi_status\") {\n          statusFrames.push(parsed.p + \"=\" + parsed.v);\n          if (statusFrames.length > 20) statusFrames.shift();\n          if (parsed.v === \"FINISHED\") finished = true;\n          else if (parsed.v === \"INCOMPLETE\") incomplete = true;\n        }\n      }\n      return {\n        consume,\n        get text() {\n          return text;\n        },\n        get finished() {\n          return finished;\n        },\n        get incomplete() {\n          return incomplete;\n        },\n        get tokenUsage() {\n          return tokenUsage;\n        },\n        get msgIds() {\n          return msgIds;\n        },\n        get thinkLen() {\n          return thinkText.length;\n        },\n        get textLen() {\n          return text.length;\n        },\n        snapshot: function() {\n          return {\n            finished,\n            incomplete,\n            userStopped,\n            thinkLen: thinkText.length,\n            textLen: text.length,\n            textTail: text.slice(-200),\n            thinkTail: thinkText.slice(-200),\n            statusFrames: statusFrames.slice()\n          };\n        }\n      };\n    }\n    function readIdleTimeout() {\n      try {\n        var v = parseInt(localStorage.getItem(\"cuckoo-xhr-idle-timeout\") || \"\", 10);\n        if (isFinite(v)) return v;\n      } catch (e) {\n      }\n      return 3e5;\n    }\n    function observeBody(body) {\n      if (!body) return;\n      var reader = body.getReader();\n      var decoder = new TextDecoder();\n      var frameDecoder = createFrameDecoder();\n      var extractor = createExtractor();\n      var dispatched = false;\n      var lastActiveAt = Date.now();\n      var idleTimer = null;\n      var idleSessionId = getSessionIdFromUrl();\n      var idleTimeout = readIdleTimeout();\n      perf.reset();\n      function stopIdleTimer() {\n        if (idleTimer) {\n          clearInterval(idleTimer);\n          idleTimer = null;\n        }\n      }\n      if (idleTimeout > 0) {\n        idleTimer = setInterval(function() {\n          if (dispatched) {\n            stopIdleTimer();\n            return;\n          }\n          if (Date.now() - lastActiveAt > idleTimeout) {\n            lastActiveAt = Date.now();\n            try {\n              window.dispatchEvent(new CustomEvent(\"cuckoo-stream-idle\", { detail: { sessionId: idleSessionId } }));\n            } catch (e) {\n            }\n          }\n        }, 5e3);\n      }\n      function feed(chunk) {\n        var _t = performance.now();\n        perf.addChunkTime();\n        perf.bytes += chunk ? chunk.length : 0;\n        var frames = frameDecoder.push(chunk);\n        perf.frames += frames.length;\n        for (var i = 0; i < frames.length; i++) {\n          var parsed = parseBlock(frames[i]);\n          if (parsed) extractor.consume(parsed);\n        }\n        perf.addConsume(performance.now() - _t);\n        if (extractor.finished && !dispatched) {\n          dispatched = true;\n          dispatch(extractor.text, resolveStatus(extractor), extractor.tokenUsage, extractor.msgIds, null, Object.assign({ path: \"feed-finished-frame\" }, extractor.snapshot()));\n          perfReport(\"fetch-stream-end\", { textLen: extractor.text.length, thinkLen: extractor.thinkLen });\n        }\n      }\n      function pump() {\n        reader.read().then(function(r) {\n          if (r.done) {\n            stopIdleTimer();\n            var tail = decoder.decode();\n            if (tail) feed(tail);\n            var rest = frameDecoder.finish();\n            for (var i = 0; i < rest.length; i++) {\n              var parsed = parseBlock(rest[i]);\n              if (parsed) extractor.consume(parsed);\n            }\n            if (!dispatched) {\n              dispatched = true;\n              dispatch(extractor.text, resolveStatus(extractor), extractor.tokenUsage, extractor.msgIds, null, Object.assign({ path: \"stream-end\" }, extractor.snapshot()));\n            }\n            return;\n          }\n          lastActiveAt = Date.now();\n          feed(decoder.decode(r.value, { stream: true }));\n          pump();\n        }).catch(function(e) {\n          stopIdleTimer();\n          if (!dispatched) {\n            dispatched = true;\n            console.log(\"[Cuckoo Code][hook] fetch stream error name=\" + (e && e.name));\n            dispatch(extractor.text, \"error\", extractor.tokenUsage, extractor.msgIds, { reason: \"stream\", name: e && e.name, sessionId: getSessionIdFromUrl() }, Object.assign({ path: \"stream-error\" }, extractor.snapshot()));\n          }\n        });\n      }\n      pump();\n    }\n    var lastCachedHeaders = \"\";\n    function cacheHeaders(hdrs) {\n      try {\n        if (!hdrs) return;\n        var lower = {};\n        for (var k in hdrs) {\n          if (Object.prototype.hasOwnProperty.call(hdrs, k)) {\n            lower[String(k).toLowerCase()] = hdrs[k];\n          }\n        }\n        if (!lower[\"authorization\"]) return;\n        var s = JSON.stringify(lower);\n        if (s === lastCachedHeaders) return;\n        lastCachedHeaders = s;\n        localStorage.setItem(\"cuckoo-ds-headers\", s);\n      } catch (e) {\n      }\n    }\n    var origFetch = window.fetch;\n    if (typeof origFetch === \"function\") {\n      window.fetch = function(input, init) {\n        var url = typeof input === \"string\" ? input : input && input.url ? input.url : input && input.href ? input.href : \"\";\n        var method = init && init.method || input && input.method || \"GET\";\n        try {\n          if (init && init.headers) {\n            var h = init.headers;\n            var obj = {};\n            if (typeof h.forEach === \"function\" && !Array.isArray(h)) {\n              h.forEach(function(v, k) {\n                obj[k] = v;\n              });\n            } else if (Array.isArray(h)) {\n              h.forEach(function(p2) {\n                obj[p2[0]] = p2[1];\n              });\n            } else {\n              obj = h;\n            }\n            cacheHeaders(obj);\n          }\n        } catch (e) {\n        }\n        var p = origFetch.apply(this, arguments);\n        if (isStopStream(url, method)) {\n          userStopped = true;\n          console.log(\"[Cuckoo Code][hook] \\u68C0\\u6D4B\\u5230 stop_stream(fetch)\\uFF0C\\u6807\\u8BB0\\u7528\\u6237\\u505C\\u6B62\");\n          return p;\n        }\n        if (!isCompletion(url, method)) return p;\n        userStopped = false;\n        var fetchSessionId = getSessionIdFromUrl();\n        return p.then(function(response) {\n          try {\n            if (response && response.ok === false) {\n              var isRL = response.status === 429;\n              dispatch(\"\", \"error\", null, null, { reason: isRL ? \"rate_limit\" : \"http\", httpStatus: response.status, sessionId: fetchSessionId }, { path: \"http-error\", httpStatus: response.status });\n            } else if (response && response.body) {\n              var ct = \"\";\n              try {\n                ct = response.headers && response.headers.get && response.headers.get(\"content-type\") || \"\";\n              } catch (e2) {\n              }\n              if (ct.indexOf(\"json\") !== -1) {\n                var sid = fetchSessionId;\n                response.clone().json().then(function(j) {\n                  var gcode = j && j.code;\n                  var biz = j && j.data && j.data.biz_code;\n                  var code = biz !== void 0 && biz !== null && biz !== 0 ? biz : gcode;\n                  if (code !== void 0 && code !== null && code !== 0) {\n                    var rl = code === 40029;\n                    console.log(\"[Cuckoo Code][hook] \\u975E\\u6D41\\u5F0F\\u9519\\u8BEF code=\" + code + (rl ? \"\\uFF08\\u64CD\\u4F5C/\\u8BF7\\u6C42\\u8FC7\\u4E8E\\u9891\\u7E41\\uFF09\" : \"\"));\n                    dispatch(\"\", \"error\", null, null, { reason: rl ? \"rate_limit\" : \"biz\", bizCode: code, sessionId: sid }, { path: \"biz-error\", bizCode: code });\n                  }\n                }).catch(function() {\n                });\n              } else {\n                observeBody(response.clone().body);\n              }\n            }\n          } catch (e) {\n          }\n          return response;\n        }, function(err) {\n          console.log(\"[Cuckoo Code][hook] fetch completion reject name=\" + (err && err.name));\n          dispatch(\"\", \"error\", null, null, { reason: \"network\", name: err && err.name, sessionId: fetchSessionId }, { path: \"network-error\", name: err && err.name });\n          throw err;\n        });\n      };\n    }\n    var origOpen = XMLHttpRequest.prototype.open;\n    var origSend = XMLHttpRequest.prototype.send;\n    var origSetRequestHeader = XMLHttpRequest.prototype.setRequestHeader;\n    var xhrInfo = /* @__PURE__ */ new WeakMap();\n    XMLHttpRequest.prototype.setRequestHeader = function(name, value) {\n      try {\n        var inf = xhrInfo.get(this) || {};\n        if (!inf.headers) inf.headers = {};\n        inf.headers[name] = value;\n        xhrInfo.set(this, inf);\n      } catch (e) {\n      }\n      return origSetRequestHeader.apply(this, arguments);\n    };\n    XMLHttpRequest.prototype.open = function(method, url) {\n      try {\n        xhrInfo.set(this, { url, method });\n      } catch (e) {\n      }\n      try {\n        if (isStopStream(url, method)) {\n          userStopped = true;\n          console.log(\"[Cuckoo Code][hook] \\u68C0\\u6D4B\\u5230 stop_stream\\uFF0C\\u6807\\u8BB0\\u7528\\u6237\\u505C\\u6B62\");\n        }\n      } catch (e) {\n      }\n      return origOpen.apply(this, arguments);\n    };\n    XMLHttpRequest.prototype.send = function(body) {\n      var info = xhrInfo.get(this);\n      if (info && info.headers) cacheHeaders(info.headers);\n      if (info && isCompletion(info.url, info.method)) {\n        userStopped = false;\n        try {\n          observeXhr(this);\n        } catch (e) {\n          console.error(\"[Cuckoo Code][hook] observeXhr \\u5F02\\u5E38: \" + e.message);\n        }\n      }\n      return origSend.apply(this, arguments);\n    };\n    function observeXhr(xhr) {\n      var lastLen = 0;\n      var frameDecoder = createFrameDecoder();\n      var extractor = createExtractor();\n      var dispatched = false;\n      var reqSessionId = getSessionIdFromUrl();\n      var xhrReads = 0;\n      var xhrRawBytes = 0;\n      perf.reset();\n      function consumeChunk() {\n        var _t = performance.now();\n        var raw;\n        try {\n          raw = xhr.responseText;\n        } catch (e) {\n          return;\n        }\n        if (typeof raw !== \"string\" || raw.length <= lastLen) return;\n        xhrReads++;\n        xhrRawBytes += raw.length;\n        perf.addChunkTime();\n        var chunk = raw.slice(lastLen);\n        lastLen = raw.length;\n        var frames = frameDecoder.push(chunk);\n        perf.frames += frames.length;\n        for (var i = 0; i < frames.length; i++) {\n          var parsed = parseBlock(frames[i]);\n          if (parsed) extractor.consume(parsed);\n        }\n        perf.addConsume(performance.now() - _t);\n        if (extractor.finished && !dispatched) {\n          dispatched = true;\n          dispatch(extractor.text, resolveStatus(extractor), extractor.tokenUsage, extractor.msgIds, null, Object.assign({ path: \"xhr-finished-frame\" }, extractor.snapshot()));\n          perfReport(\"xhr-stream-end\", { textLen: extractor.text.length, xhrReads, xhrRawBytes });\n        }\n      }\n      xhr.addEventListener(\"readystatechange\", function() {\n        if (xhr.readyState === 3 || xhr.readyState === 4) consumeChunk();\n        if (xhr.readyState === 4 && !dispatched) {\n          var rest = frameDecoder.finish();\n          for (var i = 0; i < rest.length; i++) {\n            var parsed = parseBlock(rest[i]);\n            if (parsed) extractor.consume(parsed);\n          }\n          dispatched = true;\n          var st = resolveStatus(extractor);\n          if (st === \"error\") {\n            dispatch(extractor.text, \"error\", extractor.tokenUsage, extractor.msgIds, { reason: \"xhr\", httpStatus: xhr.status, sessionId: reqSessionId }, Object.assign({ path: \"xhr-error\", httpStatus: xhr.status }, extractor.snapshot()));\n          } else {\n            dispatch(extractor.text, st, extractor.tokenUsage, extractor.msgIds, null, Object.assign({ path: \"xhr-end\" }, extractor.snapshot()));\n          }\n        }\n      });\n    }\n    try {\n      var hasCompactFlag = false;\n      try {\n        hasCompactFlag = String(location.search).indexOf(\"cuckoo-compact\") !== -1;\n      } catch (e) {\n      }\n      if (hasCompactFlag && typeof IDBObjectStore !== \"undefined\" && IDBObjectStore.prototype.put) {\n        var origPut = IDBObjectStore.prototype.put;\n        IDBObjectStore.prototype.put = function(value, key) {\n          var result = origPut.apply(this, arguments);\n          try {\n            if (this && this.name === \"history-message\" && result && typeof result.addEventListener === \"function\") {\n              result.addEventListener(\"success\", function() {\n                try {\n                  var k = key !== void 0 && key !== null ? key : value && value.key;\n                  console.log(\"[Cuckoo Code][hook] history-message \\u5199\\u5165\\u5B8C\\u6210 key=\" + k);\n                  window.dispatchEvent(new CustomEvent(\"cuckoo-idb-history-written\", { detail: { key: k } }));\n                } catch (e) {\n                }\n              });\n            }\n          } catch (e) {\n          }\n          return result;\n        };\n      }\n    } catch (e) {\n    }\n  }\n  install();\n})();\n";
const CLAUDE_HOOK = "\"use strict\";\n(() => {\n  // src/providers/hooks/shared/sse.ts\n  function createFrameDecoder() {\n    var buffer = \"\", scanFrom = 0;\n    return {\n      push: function(text) {\n        buffer += text;\n        var frames = [], re = /\\r?\\n\\r?\\n/g, offset = 0, m;\n        re.lastIndex = scanFrom;\n        while ((m = re.exec(buffer)) !== null) {\n          frames.push(buffer.slice(offset, m.index));\n          offset = m.index + m[0].length;\n        }\n        buffer = buffer.slice(offset);\n        scanFrom = Math.max(0, buffer.length - 3);\n        return frames;\n      },\n      finish: function() {\n        var frames = [];\n        if (buffer) frames.push(buffer);\n        buffer = \"\";\n        scanFrom = 0;\n        return frames;\n      }\n    };\n  }\n  function extractData(block) {\n    if (!block || !block.trim()) return null;\n    var data = null;\n    var lines = block.split(/\\r\\n|\\r|\\n/);\n    for (var i = 0; i < lines.length; i++) {\n      var line = lines[i];\n      if (line.indexOf(\"data:\") === 0) {\n        var d = line.slice(5).trim();\n        data = data == null ? d : data + \"\\n\" + d;\n      }\n    }\n    return data;\n  }\n  function parseBlock(block) {\n    var data = extractData(block);\n    if (data == null) return null;\n    try {\n      return JSON.parse(data);\n    } catch (e) {\n      return null;\n    }\n  }\n\n  // src/providers/hooks/claude.ts\n  function install() {\n    var MARKER = \"__cuckooClaudeHookInstalled__\";\n    if (window[MARKER]) return;\n    window[MARKER] = true;\n    function isCompletion(url, method) {\n      if (!url) return false;\n      if (String(method || \"GET\").toUpperCase() !== \"POST\") return false;\n      try {\n        var u = new URL(url, document.baseURI);\n        if (u.hostname.indexOf(\"claude.ai\") === -1) return false;\n        return /\\/chat_conversations\\/[^/]+\\/(completion|retry_completion)$/.test(u.pathname);\n      } catch (e) {\n        return false;\n      }\n    }\n    function dispatch(text, finished) {\n      try {\n        window.dispatchEvent(new CustomEvent(\"cuckoo-ai-response\", {\n          detail: { text: text || \"\", finished: !!finished }\n        }));\n      } catch (e) {\n      }\n    }\n    function createExtractor() {\n      var text = \"\";\n      var finished = false;\n      function consume(parsed) {\n        if (!parsed || typeof parsed !== \"object\") return;\n        var type = parsed.type;\n        if (type === \"content_block_delta\" && parsed.delta) {\n          var d = parsed.delta;\n          if (d.type === \"text_delta\" && typeof d.text === \"string\") {\n            text += d.text;\n          }\n          return;\n        }\n        if (type === \"message_stop\") {\n          finished = true;\n          return;\n        }\n        if (type === \"error\") {\n          finished = true;\n          return;\n        }\n      }\n      return {\n        consume,\n        get text() {\n          return text;\n        },\n        get finished() {\n          return finished;\n        }\n      };\n    }\n    function observeBody(body) {\n      if (!body) return;\n      var reader = body.getReader();\n      var decoder = new TextDecoder();\n      var frameDecoder = createFrameDecoder();\n      var extractor = createExtractor();\n      var dispatched = false;\n      function feed(chunk) {\n        var frames = frameDecoder.push(chunk);\n        for (var i = 0; i < frames.length; i++) {\n          var parsed = parseBlock(frames[i]);\n          if (parsed) extractor.consume(parsed);\n        }\n        if (extractor.finished && !dispatched) {\n          dispatched = true;\n          dispatch(extractor.text, true);\n        }\n      }\n      function pump() {\n        reader.read().then(function(r) {\n          if (r.done) {\n            var tail = decoder.decode();\n            if (tail) feed(tail);\n            var rest = frameDecoder.finish();\n            for (var i = 0; i < rest.length; i++) {\n              var parsed = parseBlock(rest[i]);\n              if (parsed) extractor.consume(parsed);\n            }\n            if (!dispatched) {\n              dispatched = true;\n              dispatch(extractor.text, true);\n            }\n            return;\n          }\n          feed(decoder.decode(r.value, { stream: true }));\n          pump();\n        }).catch(function() {\n          if (!dispatched) {\n            dispatched = true;\n            dispatch(extractor.text, true);\n          }\n        });\n      }\n      pump();\n    }\n    var origFetch = window.fetch;\n    if (typeof origFetch === \"function\") {\n      window.fetch = function(input, init) {\n        var url = typeof input === \"string\" ? input : input && input.url ? input.url : input && input.href ? input.href : \"\";\n        var method = init && init.method || input && input.method || \"GET\";\n        var p = origFetch.apply(this, arguments);\n        if (!isCompletion(url, method)) return p;\n        return p.then(function(response) {\n          try {\n            if (response && response.body) observeBody(response.clone().body);\n          } catch (e) {\n          }\n          return response;\n        });\n      };\n    }\n    var origOpen = XMLHttpRequest.prototype.open;\n    var origSend = XMLHttpRequest.prototype.send;\n    var xhrInfo = /* @__PURE__ */ new WeakMap();\n    XMLHttpRequest.prototype.open = function(method, url) {\n      try {\n        xhrInfo.set(this, { url, method });\n      } catch (e) {\n      }\n      return origOpen.apply(this, arguments);\n    };\n    XMLHttpRequest.prototype.send = function() {\n      var info = xhrInfo.get(this);\n      if (info && isCompletion(info.url, info.method)) {\n        try {\n          observeXhr(this);\n        } catch (e) {\n        }\n      }\n      return origSend.apply(this, arguments);\n    };\n    function observeXhr(xhr) {\n      var lastLen = 0;\n      var frameDecoder = createFrameDecoder();\n      var extractor = createExtractor();\n      var dispatched = false;\n      function consumeChunk() {\n        var raw;\n        try {\n          raw = xhr.responseText;\n        } catch (e) {\n          return;\n        }\n        if (typeof raw !== \"string\" || raw.length <= lastLen) return;\n        var chunk = raw.slice(lastLen);\n        lastLen = raw.length;\n        var frames = frameDecoder.push(chunk);\n        for (var i = 0; i < frames.length; i++) {\n          var parsed = parseBlock(frames[i]);\n          if (parsed) extractor.consume(parsed);\n        }\n        if (extractor.finished && !dispatched) {\n          dispatched = true;\n          dispatch(extractor.text, true);\n        }\n      }\n      xhr.addEventListener(\"readystatechange\", function() {\n        if (xhr.readyState === 3 || xhr.readyState === 4) consumeChunk();\n        if (xhr.readyState === 4 && !dispatched) {\n          var rest = frameDecoder.finish();\n          for (var i = 0; i < rest.length; i++) {\n            var parsed = parseBlock(rest[i]);\n            if (parsed) extractor.consume(parsed);\n          }\n          dispatched = true;\n          dispatch(extractor.text, true);\n        }\n      });\n    }\n  }\n  install();\n})();\n";
const CHATGPT_HOOK = "\"use strict\";\n(() => {\n  // src/providers/hooks/shared/sse.ts\n  function createFrameDecoder() {\n    var buffer = \"\", scanFrom = 0;\n    return {\n      push: function(text) {\n        buffer += text;\n        var frames = [], re = /\\r?\\n\\r?\\n/g, offset = 0, m;\n        re.lastIndex = scanFrom;\n        while ((m = re.exec(buffer)) !== null) {\n          frames.push(buffer.slice(offset, m.index));\n          offset = m.index + m[0].length;\n        }\n        buffer = buffer.slice(offset);\n        scanFrom = Math.max(0, buffer.length - 3);\n        return frames;\n      },\n      finish: function() {\n        var frames = [];\n        if (buffer) frames.push(buffer);\n        buffer = \"\";\n        scanFrom = 0;\n        return frames;\n      }\n    };\n  }\n  function extractData(block) {\n    if (!block || !block.trim()) return null;\n    var data = null;\n    var lines = block.split(/\\r\\n|\\r|\\n/);\n    for (var i = 0; i < lines.length; i++) {\n      var line = lines[i];\n      if (line.indexOf(\"data:\") === 0) {\n        var d = line.slice(5).trim();\n        data = data == null ? d : data + \"\\n\" + d;\n      }\n    }\n    return data;\n  }\n\n  // src/providers/hooks/chatgpt.ts\n  function install() {\n    var MARKER = \"__cuckooChatgptHookInstalled__\";\n    if (window[MARKER]) return;\n    window[MARKER] = true;\n    function isCompletion(url, method) {\n      if (!url) return false;\n      if (String(method || \"GET\").toUpperCase() !== \"POST\") return false;\n      try {\n        var u = new URL(url, document.baseURI);\n        var host = u.hostname;\n        if (host.indexOf(\"chatgpt.com\") === -1 && host.indexOf(\"chat.openai.com\") === -1) return false;\n        return /\\/backend-api\\/(?:f\\/)?conversation\\/?$/.test(u.pathname);\n      } catch (e) {\n        return false;\n      }\n    }\n    function dispatch(text, finished) {\n      try {\n        window.dispatchEvent(new CustomEvent(\"cuckoo-ai-response\", {\n          detail: { text: text || \"\", finished: !!finished }\n        }));\n      } catch (e) {\n      }\n    }\n    function parseBlock(block) {\n      var data = extractData(block);\n      if (data == null) return { data: null, done: false };\n      if (data === \"[DONE]\") return { data: null, done: true };\n      return { data, done: false };\n    }\n    function createExtractor() {\n      var text = \"\";\n      var finished = false;\n      function extractParts(content) {\n        if (!content || !Array.isArray(content.parts)) return null;\n        var out = \"\";\n        for (var i = 0; i < content.parts.length; i++) {\n          var p = content.parts[i];\n          if (typeof p === \"string\") out += p;\n          else if (p && typeof p === \"object\" && typeof p.text === \"string\") out += p.text;\n        }\n        return out;\n      }\n      function applyOp(node) {\n        if (!node || typeof node !== \"object\") return;\n        if (Array.isArray(node.v) && (node.o === \"patch\" || node.o === \"BATCH\")) {\n          for (var i = 0; i < node.v.length; i++) applyOp(node.v[i]);\n          return;\n        }\n        if (node.v && typeof node.v === \"object\" && node.v.message) {\n          var m = node.v.message;\n          var role = m.author && m.author.role;\n          if (role === \"assistant\") {\n            var snap = extractParts(m.content);\n            if (snap !== null) text = snap;\n          }\n          return;\n        }\n        if (typeof node.p === \"string\" && node.p !== \"\") {\n          if (node.p === \"/message/status\" && node.v === \"finished_successfully\") {\n            finished = true;\n            return;\n          }\n          if (node.p === \"/message/end_turn\" && node.v === true) {\n            finished = true;\n            return;\n          }\n          if (/\\/message\\/content\\/parts\\/\\d+$/.test(node.p)) {\n            if (typeof node.v === \"string\") {\n              if (node.o === \"append\" || node.o === \"add\") text += node.v;\n              else text = node.v;\n            }\n          }\n          return;\n        }\n        if (typeof node.v === \"string\") {\n          text += node.v;\n          return;\n        }\n        if (node.type === \"message_stream_complete\" || node.type === \"message_stream_completed\") {\n          finished = true;\n        }\n      }\n      return {\n        consume: function(parsed) {\n          applyOp(parsed);\n        },\n        markDone: function() {\n          finished = true;\n        },\n        get text() {\n          return text;\n        },\n        get finished() {\n          return finished;\n        }\n      };\n    }\n    function observeBody(body) {\n      if (!body) return;\n      var reader = body.getReader();\n      var decoder = new TextDecoder();\n      var frameDecoder = createFrameDecoder();\n      var extractor = createExtractor();\n      var dispatched = false;\n      function flushFrame(frame) {\n        var r = parseBlock(frame);\n        if (r.done) {\n          extractor.markDone();\n          return;\n        }\n        if (r.data == null) return;\n        var parsed;\n        try {\n          parsed = JSON.parse(r.data);\n        } catch (e) {\n          return;\n        }\n        extractor.consume(parsed);\n      }\n      function feed(chunk) {\n        var frames = frameDecoder.push(chunk);\n        for (var i = 0; i < frames.length; i++) flushFrame(frames[i]);\n        if (extractor.finished && !dispatched) {\n          dispatched = true;\n          dispatch(extractor.text, true);\n        }\n      }\n      function pump() {\n        reader.read().then(function(r) {\n          if (r.done) {\n            var tail = decoder.decode();\n            if (tail) feed(tail);\n            var rest = frameDecoder.finish();\n            for (var i = 0; i < rest.length; i++) flushFrame(rest[i]);\n            if (!dispatched) {\n              dispatched = true;\n              dispatch(extractor.text, true);\n            }\n            return;\n          }\n          feed(decoder.decode(r.value, { stream: true }));\n          pump();\n        }).catch(function() {\n          if (!dispatched) {\n            dispatched = true;\n            dispatch(extractor.text, true);\n          }\n        });\n      }\n      pump();\n    }\n    var origFetch = window.fetch;\n    if (typeof origFetch === \"function\") {\n      window.fetch = function(input, init) {\n        var url = typeof input === \"string\" ? input : input && input.url ? input.url : input && input.href ? input.href : \"\";\n        var method = init && init.method || input && input.method || \"GET\";\n        var p = origFetch.apply(this, arguments);\n        if (!isCompletion(url, method)) return p;\n        return p.then(function(response) {\n          try {\n            if (response && response.body) observeBody(response.clone().body);\n          } catch (e) {\n          }\n          return response;\n        });\n      };\n    }\n    var origOpen = XMLHttpRequest.prototype.open;\n    var origSend = XMLHttpRequest.prototype.send;\n    var xhrInfo = /* @__PURE__ */ new WeakMap();\n    XMLHttpRequest.prototype.open = function(method, url) {\n      try {\n        xhrInfo.set(this, { url, method });\n      } catch (e) {\n      }\n      return origOpen.apply(this, arguments);\n    };\n    XMLHttpRequest.prototype.send = function() {\n      var info = xhrInfo.get(this);\n      if (info && isCompletion(info.url, info.method)) {\n        try {\n          observeXhr(this);\n        } catch (e) {\n        }\n      }\n      return origSend.apply(this, arguments);\n    };\n    function observeXhr(xhr) {\n      var lastLen = 0;\n      var frameDecoder = createFrameDecoder();\n      var extractor = createExtractor();\n      var dispatched = false;\n      function flushFrame(frame) {\n        var r = parseBlock(frame);\n        if (r.done) {\n          extractor.markDone();\n          return;\n        }\n        if (r.data == null) return;\n        var parsed;\n        try {\n          parsed = JSON.parse(r.data);\n        } catch (e) {\n          return;\n        }\n        extractor.consume(parsed);\n      }\n      function consumeChunk() {\n        var raw;\n        try {\n          raw = xhr.responseText;\n        } catch (e) {\n          return;\n        }\n        if (typeof raw !== \"string\" || raw.length <= lastLen) return;\n        var chunk = raw.slice(lastLen);\n        lastLen = raw.length;\n        var frames = frameDecoder.push(chunk);\n        for (var i = 0; i < frames.length; i++) flushFrame(frames[i]);\n        if (extractor.finished && !dispatched) {\n          dispatched = true;\n          dispatch(extractor.text, true);\n        }\n      }\n      xhr.addEventListener(\"readystatechange\", function() {\n        if (xhr.readyState === 3 || xhr.readyState === 4) consumeChunk();\n        if (xhr.readyState === 4 && !dispatched) {\n          var rest = frameDecoder.finish();\n          for (var i = 0; i < rest.length; i++) flushFrame(rest[i]);\n          dispatched = true;\n          dispatch(extractor.text, true);\n        }\n      });\n    }\n  }\n  install();\n})();\n";

export { DEEPSEEK_HOOK, CLAUDE_HOOK, CHATGPT_HOOK };
